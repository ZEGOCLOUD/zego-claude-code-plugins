# RTC(实时音视频、实时语音、低延迟直播、Video Call、Voice Call、Live Streaming) Integration Guide

## General Instructions

## Platform-Specific Instructions
**Web SDK Integration**
- Use npm to install the ZEGO Web SDK and import the SDK into the project
- Must use the Web's push stream and pull stream interfaces, because the push stream and pull stream interfaces of Web are based on WebRTC, which is different from other platforms.